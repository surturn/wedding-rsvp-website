import { Heart } from 'lucide-react'

// A subtle heart motif to replace the previous sign language icon
export function HandSignIcon({ className = '' }: { className?: string }) {
  return <Heart className={className} aria-label="Heart Icon" />
}

export function HeartHandsIcon({ className = '' }: { className?: string }) {
  return <Heart className={className} aria-label="Heart Icon" />
}
